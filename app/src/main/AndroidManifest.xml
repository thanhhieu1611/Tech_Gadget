<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.newtechnologies">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewTechnologies">
        <activity android:name=".Activity_Rocket"/>
        <activity android:name=".Activity_RenewableEnergy" />
        <activity android:name=".Activity_BlockChain" />
        <activity android:name=".Activity_AutomaticCar" />
        <activity android:name=".Activity_AirTaxi" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>